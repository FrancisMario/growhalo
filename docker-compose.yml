services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: growhalo
      POSTGRES_PASSWORD: growhalo
      POSTGRES_DB: growhalo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
